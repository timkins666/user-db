[project]
name = "user-db"
version = "0.1.0"
description = "Root project for Python workspaces"
requires-python = ">=3.13"
dependencies = [
    "backend-fastapi",
]

[tool.uv.sources]
backend-fastapi = { workspace=true }

[tool.uv.workspace]
members = [
    "backend-fastapi",
]
